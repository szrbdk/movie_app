enum AuthStatus { authenticated, unauthenticated }
enum AuthScreenStatus { loading, authorized, unauthorized }
enum ImageSize { w92, w154, w185, w342, w500, w780, original }
enum FilterType { popular, trending, anticipated }
